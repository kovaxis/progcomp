#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < n; i++)
#define invrep(i, n) for (int i = n - 1; i >= 0; i--)

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
}
